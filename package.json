{
  "name": "journal-impact-factor",
  "version": "1.0.0",
  "description": "Journal Impact Factor 조회 웹 애플리케이션",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start"
  }
}
